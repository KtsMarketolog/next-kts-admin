# Импорт остатков из почты

Импорт остатков обновляет только наличие товаров. Он не создает товары, категории, подкатегории и бренды, не меняет названия, цены, картинки и структуру каталога.

## Env

Можно использовать отдельные `STOCK_MAIL_*`, либо уже настроенные `SMTP_USER` / `SMTP_PASSWORD`.
Если `STOCK_MAIL_USER` и `STOCK_MAIL_PASSWORD` не заданы, импорт возьмет `SMTP_USER` и `SMTP_PASSWORD`.

```env
STOCK_MAIL_HOST=
STOCK_MAIL_PORT=993
STOCK_MAIL_SECURE=true
STOCK_MAIL_USER=
STOCK_MAIL_PASSWORD=
STOCK_MAIL_ALLOWED_FROM=
STOCK_MAIL_SUBJECT=
STOCK_MAIL_FILENAME_PREFIX=Остатки
STOCK_MAIL_SCAN_LIMIT=300
STOCK_MAIL_PROCESSED_FOLDER=Processed
STOCK_MAIL_ERROR_FOLDER=ImportErrors
```

Для Яндекс.Почты host можно не указывать, по умолчанию используется `imap.yandex.ru`.
Если `STOCK_MAIL_ALLOWED_FROM` не задан, разрешенным отправителем считается почта из `STOCK_MAIL_USER` или `SMTP_USER`.

Пароль почты хранится только в env. В код его не добавлять.

## Формат Excel

Поддерживается только `.xlsx`. По умолчанию принимаются файлы, имя которых начинается с `Остатки`, например `Остатки_2026-05-11.xlsx`. Префикс задается через `STOCK_MAIL_FILENAME_PREFIX`.

Проверяются последние письма в ящике, включая прочитанные. По умолчанию просматриваются последние `300` писем, значение можно изменить через `STOCK_MAIL_SCAN_LIMIT`. Из всех подходящих писем импортируется только самое свежее по дате письма.

Первый лист должен содержать колонки:

| Наименование | Остаток | Ожидается |
| --- | ---: | --- |
| Дверь липа 1800х700 | 12 | нет |
| Дверь стеклянная бронза 1900х700 | 0 | да |

Правила:

- `Наименование` обязательно. Товар ищется по точному названию после `trim`, замены неразрывных пробелов и схлопывания лишних пробелов.
- `Остаток` обязателен, целое число `>= 0`. Также принимается название колонки `Остатки`.
- `Ожидается` необязательно. Если колонка отсутствует или ячейка пустая, значение считается `нет`. Если заполнено, принимаются: `да`, `нет`, `true`, `false`, `1`, `0`.
- Если товар не найден или найдено несколько товаров с одинаковым названием, строка не обновляется.
- Товары, которых нет в файле, не обнуляются.

## Запуск

Вручную из консоли:

```bash
npm run stock:import-email
```

В админке:

`/admin/site?section=catalog` → блок `Импорт остатков` → `Проверить почту сейчас`.

API для ручного запуска:

```http
POST /api/admin/stock-import/check-email
```

Endpoint доступен только авторизованному администратору.

## Cron на VPS

Пример запуска каждые 10 минут:

```cron
*/10 * * * * cd /home/deploy/apps/PROJECT/current && npm run stock:import-email >> /var/log/stock-import.log 2>&1
```

Путь заменить на фактический путь проекта.

## Логи

Результаты сохраняются в `stock_import_logs`:

- дата импорта;
- имя файла;
- отправитель;
- тема письма;
- статус;
- всего строк;
- обновлено;
- не найдено;
- ошибок;
- JSON со списком ошибок.

Для всего почтового цикла и ручного Excel-импорта используется один PostgreSQL
session advisory lock `kts-stock-import`. Он удерживается до закрытия IMAP-сессии,
не истекает через 30 минут и не позволяет другому новому процессу начать второй
импорт. Если занят, почтовый вызов возвращает `status: "busy"`, не меняет письма
и не переносит их в `ImportErrors`. Админка и CLI явно сообщают о пропуске.

Таблица `stock_import_locks` с прежним ключом `email` и временем 30 минут оставлена
как дополнительная защита на время перехода со старого кода. Её занятость для
нового обработчика также означает безопасный пропуск, а не ошибочный файл.
Настоящий некорректный Excel по-прежнему обрабатывается как ошибка импорта.

### Первое развёртывание изменения блокировок

Старый процесс не знает новый session lock. Перед переключением нужно временно
приостановить фактический планировщик проверки остатков и не запускать ручной
импорт, дождаться завершения старого почтового цикла, затем развернуть код и
возобновить прежнее расписание. Одного rolling reload недостаточно, если старый
почтовый запрос ещё выполняется. Пути/команды планировщика сначала выясняются
на конкретном сервере; почтовые секреты не выводятся. Таблица блокировки не
очищается вручную, письма ради проверки не переносятся.
